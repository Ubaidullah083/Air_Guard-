import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ────────────────────────────────────────────
  static const primary = Color(0xFF1565C0);
  static const primaryLight = Color(0xFF42A5F5);

  // ── Status ───────────────────────────────────────────
  static const good = Color(0xFF059669);
  static const goodBg = Color(0xFFD1FAE5);
  static const goodText = Color(0xFF065F46);

  static const moderate = Color(0xFFD97706);
  static const moderateBg = Color(0xFFFEF3C7);
  static const moderateText = Color(0xFF92400E);

  static const warning = Color(0xFFEA580C);
  static const warningBg = Color(0xFFFFF7ED);
  static const warningText = Color(0xFF9A3412);

  static const critical = Color(0xFFDC2626);
  static const criticalBg = Color(0xFFFEE2E2);
  static const criticalText = Color(0xFF991B1B);

  static const offline = Color(0xFF94A3B8);
  static const offlineBg = Color(0xFFF1F5F9);
  static const offlineText = Color(0xFF64748B);

  // ── Light theme ──────────────────────────────────────
  static const lightBg1 = Color(0xFFCCE8F6);
  static const lightBg2 = Color(0xFFD8F0E8);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightCardBorder = Color(0xFFF1F5F9);
  static const lightText = Color(0xFF0A2540);
  static const lightSubtext = Color(0xFF6A8FA8);
  static const lightMuted = Color(0xFF94A3B8);
  static const lightNav = Color(0xAAFFFFFF);
  static const lightInput = Color(0xFFF8FAFC);
  static const lightInputBorder = Color(0xFFE2E8F0);
  static const lightDivider = Color(0xFFF1F5F9);

  // ── Dark theme ───────────────────────────────────────
  static const darkBg = Color(0xFF0D1B2E);
  static const darkBg2 = Color(0xFF0A1628);
  static const darkCard = Color(0x10FFFFFF);
  static const darkCardBorder = Color(0x1AFFFFFF);
  static const darkText = Color(0xFFE2E8F0);
  static const darkSubtext = Color(0xFF4A6880);
  static const darkMuted = Color(0xFF2A3F52);
  static const darkNav = Color(0xEB0D1B2E);
  static const darkInput = Color(0x0DFFFFFF);
  static const darkInputBorder = Color(0x1AFFFFFF);
  static const darkDivider = Color(0x14FFFFFF);

  // ── Dark status ──────────────────────────────────────
  static const darkGood = Color(0xFF34D399);
  static const darkGoodBg = Color(0x1A34D399);
  static const darkModerate = Color(0xFFFBBF24);
  static const darkModerateBg = Color(0x1AFBBF24);
  static const darkWarning = Color(0xFFFB923C);
  static const darkWarningBg = Color(0x1AFB923C);
  static const darkCritical = Color(0xFFF87171);
  static const darkCriticalBg = Color(0x1AF87171);
}
